# UN Nations Quiz (MVP)

## Run locally
1. Install dependencies:
   ```bash
   npm install
   ```
2. Start dev server:
   ```bash
   npm run dev
   ```
3. Open http://localhost:3000

## Add questions
Edit `lib/questions.ts` and append to `BANK`.
